# 📦 一键打包 APK 说明

## Windows 电脑打包方法

### 方法一：GitHub Actions（推荐，最简单）
1. 把整个文件夹上传到你的 GitHub 仓库
2. 打开 GitHub → Actions → Build APK → Run workflow
3. 等 20-30 分钟，下载 APK ✓

### 方法二：WSL 本地打包
1. 安装 WSL2（Windows 应用商店搜 Ubuntu）
2. 打开 WSL 终端，运行：
```bash
sudo apt update
sudo apt install python3 python3-pip git
pip install buildozer
cd /mnt/c/Users/你的用户名/Desktop/OfficeApp
buildozer android debug
```
3. APK 在 `bin/` 文件夹里

### 方法三：Docker 打包
```bash
docker run --name buildozer -v %cd%:/app -w /app kivy/buildozer:latest buildozer android debug
```

## 自定义品牌
你需要在以下地方改成你的名字：
- `buildozer.spec` → title / package.domain
- `main.py` → 第 152 行的 "你的品牌名称"
- `icon.png` → 替换成你自己的图标（512x512）
- `splash.png` → 替换成启动画面图

## 打包完成后
APK 文件在 `bin/` 目录，直接传到手机上安装即可 📱
